name: toyo_voice_party
description: TOYO Voice Party - Arabic RTL social voice rooms, agencies, gifts and VIP.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  shared_preferences: ^2.3.2
  connectivity_plus: ^6.0.5
  intl: ^0.19.0
  google_fonts: ^6.2.1
  agora_rtc_engine: ^6.6.4
  socket_io_client: ^3.1.6
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
