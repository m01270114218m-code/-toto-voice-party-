# MT2 Voice Party

Clean Flutter project rebuilt from scratch around the MT2 visual/resource structure.

Legacy application code, backend, and migrations were removed from the main project.

MT2 assets belong under `mobile/assets/mt2/` and are declared by Flutter in `mobile/pubspec.yaml`.