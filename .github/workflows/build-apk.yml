name: Build Pharaoh Party Android APK

on:
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  build:
    name: Build Pharaoh Party release APK
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout Pharaoh Party
        uses: actions/checkout@v5

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Verify Pharaoh Party files
        working-directory: mobile
        run: |
          test -f pubspec.yaml
          test -f lib/main.dart
          grep -q "PharaohPartyApp" lib/main.dart
          grep -q "فرعون بارتي" lib/main.dart

      - name: Get Flutter packages
        working-directory: mobile
        run: flutter pub get

      - name: Create Android project if missing
        working-directory: mobile
        run: |
          if [ ! -f android/gradlew ]; then
            flutter create --platforms=android .
          fi

      - name: Build Pharaoh Party release APK
        working-directory: mobile
        run: flutter build apk --release

      - name: Verify Pharaoh Party APK
        run: |
          test -s mobile/build/app/outputs/flutter-apk/app-release.apk
          ls -lh mobile/build/app/outputs/flutter-apk/app-release.apk

      - name: Upload NEW template APK
        uses: actions/upload-artifact@v4
        with:
          name: Pharaoh-Party-release-apk
          path: mobile/build/app/outputs/flutter-apk/app-release.apk
          if-no-files-found: error
          retention-days: 30
